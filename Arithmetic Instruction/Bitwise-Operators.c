main()
{
    int x;
    // And Operator;

    // x=23&56;
    // =>Result is 16.


    // OR Operator;
    // x=23|56;

    // =>Result is 63.

    

    // XOR Operator;

    // x=23^56;
    // =>Result is 47;



    // Right Shift;

    // x=56>>1;
    // =>Result is 28;   Two zero i.e(00)added in the left side such that the two digit from right side get removed and the total digit still remain 16.
    


    // Left Shift;
    // x=56<<3;          Three zero i.e(000)added in the right side such that the three digit from left side get removed and the total digit still remain 16.
    // =>Result is 448;
     
    printf("The binary number Is %d",x);
}