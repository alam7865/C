#include<stdio.h>
main()
{
    // char s[10]={'S','A','U','R','A','B','H','\0'};

    // Another way of writing string;
    char s[10]="SAURABH";
    int i;
    // for(i=0;s[i]!='\0';i++)
        // printf("%c",s[i]);
        printf("%s",s);
        // puts(s);




        // Taking sentence as input from user;

    char s[20];
    // scanf("%s",s);
    gets(s);
    // printf("%s",s);
    // gets(s);
    puts(s);
    
}